# RecetIA — Backend (FastAPI) · Fase 0
# Instalar:  pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
pydantic>=2.9.0
pydantic-settings>=2.5.0
email-validator>=2.2.0
pyjwt>=2.9.0
bcrypt>=4.2.0
python-multipart>=0.0.18
httpx>=0.27.0
# >=0.69 garantiza client.messages.parse(output_format=...) (salida estructurada)
anthropic>=0.69.0,<1.0.0
# OCR de ticket por visión cuando OCR_PROVIDER=openai (Fase 5).
openai>=1.40.0

# Forecasting (Fase 4) — el backend sirve /comercio/forecast con el modelo XGBoost.
xgboost>=2.0
pandas>=2.0
numpy>=1.26
scikit-learn>=1.3

# OCR de ticket (Fase 5). Requiere además el binario Tesseract instalado en el SO.
pytesseract>=0.3.10
pillow>=10.0
